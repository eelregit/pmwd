---
trigger: always_on
---

- ALWAYS activate the "sto" conda environment by running "mamba activate sto" before running any Python script.
- NEVER run Python script or install Python packages in the "base" conda environment.